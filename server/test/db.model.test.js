const { expect } = require('chai');
const { describe } = require('mocha');
const {
    findTicker
} = require("../models/db.model");

describe("find ticker in db", function () {
    it("", async function () {
        //expect(document).to.be.an("array");
    // some = some
    // expect some to
    });
});